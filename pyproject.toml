[project]
name = "jetblack-ksqldb"
version = "0.1.0"
description = "Let's go psycho!"
authors = [{name = "Rob Blackbourn", email = "rob.blackbourn@gmail.com"}]
readme = "README.md"
dependencies = [
    "httpx[http2]<1,>=0.28.1"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
